# Pages

Each file represents a screen in the app (JoinPage, GamePage, WinPage).  
These are the main routes of the single-page application.  
Pages can use components and state/hooks to display the UI.